development:
    adapter: mysql2
    database: retro_market_local_development
    encoding: utf8
    username: root
    password: password
    host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
    adapter: mysql2
    database: retro_market_local_test
    encoding: utf8
    username: root
    password: password
    host: localhost

staging:
    adapter: mysql2
    database: retro_market_local_staging
    encoding: utf8
    username: root
    password: password
    host: localhost

production:
    adapter: mysql2
    database: retro_market_local_development
    encoding: utf8
    username: root
    password: password
    host: localhost

cucumber:
  <<: *test
