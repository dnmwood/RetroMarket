<div class="game-form">
  <%= form_for [@game] do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title, :class => "form-control" %>

    <%= f.submit "Submit", :class=>"btn btn-primary" %>
  <% end %>
</div>
